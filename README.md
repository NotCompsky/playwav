Tiny utility to play (most) WAV files.

It is used in one of my Python projects, because it can be loaded in the same process with CPython - the alternatives requiring invocation by the command line.
